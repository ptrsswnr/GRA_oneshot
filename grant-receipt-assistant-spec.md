# ข้อกำหนดระบบ (Spec) — 🧾 Grant Receipt Assistant ระบบตรวจใบเสร็จทุนวิจัย

> **เอกสารนี้คือใบสั่งงานของระบบ Grant Receipt Assistant** — บอกว่าระบบต้องทำอะไรได้บ้าง หน้าตาเป็นอย่างไร
> และเก็บข้อมูลไว้ที่ไหน เขียนขึ้นจากการรวม `SCOPE.md` เดิม (ขอบเขต Week 6) เข้ากับสถานะจริงของโค้ด
> ในโปรเจกต์ `Grant Receipt Assistant` ที่ทำเสร็จไปแล้ว (อ่าน `CLAUDE.md`/`ACL.md`/`BACKLOG.md` ของ
> โปรเจกต์เดิมแล้วแก้ไขให้ตรงกับของจริง ไม่ใช่แค่ก็อปสเปกเก่ามาทั้งดุ้น) **ใช้เอกสารนี้สั่งสร้างระบบใหม่
> ทั้งระบบในโฟลเดอร์นี้แบบทีเดียวจบ** (ไม่ใช่ทยอยสร้างทีละสัปดาห์เหมือนโปรเจกต์เดิม)

---

## 0. วิธีใช้เอกสารนี้ และข้อกำหนดทางเทคนิค

### 0.1 เอกสารนี้ใช้อย่างไร

1. วางไฟล์นี้ไว้ใน **โฟลเดอร์โปรเจกต์นี้** (`GRA_oneshot/`)
2. เปิด Claude Code ในโฟลเดอร์นี้ สั่งให้อ่านไฟล์นี้ก่อน แล้วสรุปกลับมาเป็นข้อ ๆ ว่า
   1) ระบบนี้ใช้ทำอะไร 2) มีหน้าจอกี่หน้า อะไรบ้าง 3) ผู้ใช้มีกี่บทบาท ทำอะไรได้/ไม่ได้
   **ยังไม่ต้องเขียนโค้ด**
3. **ถ้าสรุปกลับมาไม่ตรงกับเอกสารนี้ ให้แก้เอกสารนี้ก่อน อย่าเพิ่งสร้างหน้าจอ**
4. จากนั้นให้ AI **เสนอแผนทีมผู้ช่วย (subagent) 3 ตัว** ที่จะใช้สร้างระบบนี้ — งานครบ, ใช้โมเดลต่างกัน
   (เช่น ตัวเขียนโครงสร้าง/ฐานข้อมูล, ตัวเขียนหน้าจอ/UI, ตัวรีวิวความปลอดภัย/ACL), แต่ละตัวมีข้อห้ามชัดเจน
   (เช่น "ห้ามแก้ Security Rules", "ห้ามเปลี่ยนชื่อฟิลด์ที่ตกลงกันไว้ในเอกสารนี้") — **ให้คนตรวจแผนก่อน
   ค่อยสั่งสร้างจริง**

> ⚠️ **กติกาสำคัญสำหรับ AI ที่อ่านเอกสารนี้:** ทำเฉพาะสิ่งที่เขียนไว้ในเอกสารนี้ **ห้ามเพิ่มฟีเจอร์ที่ไม่ได้
> ระบุ** (ดูหัวข้อ 8 "นอกขอบเขต") และ **ห้ามใส่คีย์ AI/คีย์ลับใด ๆ ลงในโค้ดที่ commit** (ดูหัวข้อ 9)

### 0.2 ข้อกำหนดทางเทคนิค (บังคับ ห้ามเปลี่ยนเอง)

| หัวข้อ | ข้อกำหนด |
|---|---|
| **หน้าเว็บ** | HTML · CSS · JavaScript ธรรมดา **ห้ามใช้ framework** (React/Vue/Next.js/Tailwind ฯลฯ) — ตรงกับที่โปรเจกต์เดิมทำจริง |
| **โครงไฟล์** | หลายไฟล์ `.html` ไฟล์ละหนึ่งหน้าจอ, CSS อยู่ใน `css/`, JS อยู่ใน `js/` |
| **เซิร์ฟเวอร์** | ไม่มีเซิร์ฟเวอร์ที่เขียนเอง — หน้าเว็บคุยกับ Firebase ตรง ๆ ผ่าน Firebase JS SDK |
| **ฐานข้อมูล** | **Firestore** — โครงสร้าง nested ตามความเป็นเจ้าของข้อมูล (ดูหัวข้อ 3) ไม่ใช่ flat/FK |
| **ล็อกอิน** | **Firebase Authentication** (email/password) |
| **ยามเฝ้าข้อมูล** | **Firestore Security Rules** — ต้อง deploy จริงและพิสูจน์ได้ว่าบัญชี A เปิดข้อมูลของบัญชี B ไม่ได้ (ดูหัวข้อ 6) |
| **ไฟล์แนบ** | Firebase Storage **ไม่ได้ provision ไว้** (ต้องอัปเกรด Blaze plan) — ใช้ **Google Drive ของผู้ใช้เอง** แทน (OAuth `drive.file` scope) เก็บแค่ลิงก์ไฟล์ไว้ใน Firestore แทนไฟล์จริง |
| **AI ที่ใช้จริง** | OpenRouter API, โมเดล `google/gemini-2.5-flash-lite` (vision) — อ่านใบเสร็จเป็นรูป/PDF หน้าแรก แล้วคืนค่า JSON (ผู้ขาย/วันที่/จำนวนเงิน/หมวด) |
| **นำขึ้นออนไลน์** | **Firebase Hosting** |
| **ภาษาบนหน้าจอ** | ภาษาไทยทั้งหมด (ข้อความ/label/ปุ่ม/คำเตือน) |
| **ชื่อโค้ด** | ชื่อไฟล์, ชื่อตัวแปร, ชื่อฟังก์ชัน, DOM id/name ที่ JS เรียกใช้ **ต้องเป็นภาษาอังกฤษ** (เช่น `warningBox`, `nextReceiptId()`) — Thai อยู่แค่ในข้อความที่ผู้ใช้เห็นและ comment เท่านั้น (ต่างจาก LeaveEasy ซึ่งใช้ชื่อโค้ดภาษาไทย — โปรเจกต์นี้ยืนยันภาษาอังกฤษเท่านั้น) |

---

## 1. ระบบนี้แก้ปัญหาอะไร ให้ใคร

Grant Receipt Assistant ช่วยให้ **นักวิจัยเจ้าของทุน** อัปโหลดใบเสร็จ/หลักฐานการเบิกจ่ายเงินทุนวิจัย
แล้วเห็นผลตรวจทันที (**ผ่าน / ต้องแก้ไข / ไม่เข้าเงื่อนไข**) พร้อมคำอธิบายภาษาที่เข้าใจง่าย
**ก่อน**ส่งเอกสารจริงให้ฝ่ายการเงิน — ลดรอบการแก้ไขเอกสารกลับไปกลับมา และช่วยให้นักวิจัยที่ไม่มีพื้นฐาน
บัญชี/การเงินเข้าใจได้ว่าทำไมใบเสร็จถึงผ่านหรือไม่ผ่าน

Admin (ผู้ดูแลระบบ) จัดการ **แหล่งทุน** และ **กฎการเบิกจ่ายของแต่ละแหล่งทุน** (หมวด/เพดาน/เอกสารที่ต้องแนบ)
ให้ตรงกับระเบียบจริงของมหาวิทยาลัย และดูภาพรวม/รายละเอียดใบเสร็จของนักวิจัยทุกคนเพื่อทำรายงาน

**เส้นทางหลักของระบบ:** นักวิจัยสร้างโครงการ → อัปโหลดใบเสร็จ (AI อ่านข้อมูลให้) → ระบบตรวจตามกฎของ
แหล่งทุนนั้น → นักวิจัยเห็นสถานะ + คำอธิบาย → (ถ้าไม่ผ่าน) แก้ไขแล้วอัปโหลดใหม่

---

## 2. ผู้ใช้ 2 บทบาท

| บทบาท | ค่าที่เก็บ | ทำอะไรได้ | ทำอะไรไม่ได้ |
|---|---|---|---|
| 🔬 **นักวิจัย** (Researcher) | `users/{uid}.isAdmin == false` (ค่าเริ่มต้นตอนสมัคร) | สร้าง/ดูโครงการวิจัยของตัวเอง · อัปโหลด/ดู/**ลบ**ใบเสร็จของตัวเอง · ดูรายชื่อแหล่งทุน (อ่านอย่างเดียว ใช้เลือกตอนสร้างโครงการ) | ดู/แก้ไข/ลบข้อมูลโครงการหรือใบเสร็จของนักวิจัยคนอื่น (ห้ามแม้เดา ID ถูก — บังคับที่ระดับ Firestore rules) · เพิ่ม/แก้ไข/ลบแหล่งทุนหรือกฎ (`ruleItems`) · ตั้ง `isAdmin` ให้ตัวเอง · เข้าหน้า Admin Dashboard/แหล่งทุน (ไม่เห็นแม้แต่ในเมนู) |
| 🛡️ **Admin** | `users/{uid}.isAdmin == true` (ตั้งได้เฉพาะผ่าน Firebase Console ด้วยมือ ไม่มี UI self-promote) | **อ่าน**โครงการ/ใบเสร็จของนักวิจัย**ทุกคน**ผ่าน Admin Dashboard (รวมชื่อ-อีเมลเจ้าของ) · เพิ่ม/แก้ไข/**ลบ**แหล่งทุนและกฎ (`ruleItems`) | **เขียน/แก้ไข/ลบ**ข้อมูลโครงการหรือใบเสร็จของนักวิจัยคนใดเลย (สิทธิ์เป็นอ่านอย่างเดียวล้วน ๆ นอกจาก fundSources/ruleItems) · สร้าง/แก้ไขโครงการหรือใบเสร็จแทนนักวิจัย |

> 📌 สมัครสมาชิกใหม่ทุกบัญชีได้ `isAdmin: false` เสมอ ไม่มี role อื่นนอกจากนี้ (ไม่มี "เจ้าหน้าที่การเงิน"
> หรือ "ผู้อนุมัติทุน" — ดูหัวข้อ 8 นอกขอบเขต)

---

## 3. Data model — Firestore (nested ตามความเป็นเจ้าของ)

```
users/{userId}                                            { fullName, email, isAdmin, privacyConsentAcceptedAt }
users/{userId}/projects/{projectId}                       { projectName, fundSourceId, ownerUserId, createdAt }
users/{userId}/projects/{projectId}/budgetItems/{budgetItemId}          { categoryName, estimatedAmount, unit,
                                                             note, createdAt }
users/{userId}/projects/{projectId}/receipts/{receiptId}  { ownerUserId, category, amount, vendorName, date,
                                                             status, aiExplanation, uploadedAt, fileReference,
                                                             evidenceChainId }
users/{userId}/projects/{projectId}/receipts/{receiptId}/files/{fileId}      { fileReference (ลิงก์ Google Drive),
                                                             documentTypeId (nullable, อ้าง documentTypes/{id}),
                                                             extractedFields ({ [fieldName]: value } ที่ผู้ใช้
                                                             ยืนยัน/แก้แล้ว — ดู FS-08), extractedFieldsWasAi (bool) }
users/{userId}/projects/{projectId}/receipts/{receiptId}/agentLogs/{logId}   { usedAi, model, evidenceChainId,
                                                             documentTypeIds, finalExplanation, durationMs }
fundSources/{fundSourceId}                                 { fundSourceName, fundSourceCode }
fundSources/{fundSourceId}/ruleVersions/{ruleVersionId}    { versionLabel, isActive, importedByUserId }
fundSources/{fundSourceId}/ruleVersions/{id}/ruleItems/{ruleItemId}  { categoryName, rateType, rateAmount, unit,
                                                             requiredEvidenceType, note, evidenceChainId }
documentTypes/{documentTypeId}                             { documentName, formCode, requiredFields[], validityChecks[] }
evidenceChains/{evidenceChainId}                           { chainName, steps[], conditionalSteps[] }
```

- ดูใบเสร็จรวมข้ามทุกโครงการ ("ใบเสร็จของฉัน" / Admin Dashboard) ต้องใช้ `collectionGroup("receipts")`
  พร้อม field `ownerUserId` ที่ฝังไว้ในเอกสารแต่ละใบ (ไม่ใช่ path — collection-group query ไม่มี
  `{userId}` ให้เทียบตรง ๆ)
- **`receipt.status` มีได้แค่ 3 ค่า** ตั้งครั้งเดียวตอนสร้าง ไม่มีการแก้ไขสถานะทีหลัง:
  `ผ่าน` / `ต้องแก้ไข` / `ไม่เข้าเงื่อนไข`
- `rateType` ของ `ruleItem` เป็นหนึ่งใน: `เพดานตามจริง` / `เหมาจ่าย` / `ต่อหน่วย` / `ตามข้อเสนอโครงการ`
- ต้องมี Firestore index สำหรับ collection-group query (`ownerUserId` และ `uploadedAt`) — ใส่ใน
  `firestore.indexes.json` และ deploy จริง
- `budgetItems` เป็น subcollection ของ **แต่ละโครงการ** (ไม่ใช่ของแหล่งทุน) — ตัวเลขประมาณการเป็นของ
  โครงการนั้นโครงการเดียว โครงการอื่นแม้ใช้แหล่งทุนเดียวกันก็มีตัวเลขของตัวเองแยกกัน

### 3.1 ข้อมูลตั้งต้นจริง (seed data — ใส่ให้ตรงเป๊ะ ห้ามแต่งขึ้นใหม่)

> ตัวเลข/รายชื่อเอกสารทั้งหมดด้านล่างมาจาก **"หลักการและวิธีการกำหนดค่าใช้จ่ายและงบประมาณ โครงการวิจัย
> ที่เสนอขอรับเงินอุดหนุนจากมหาวิทยาลัยแม่ฟ้าหลวง ประจำปีงบประมาณ พ.ศ. 2570"** และ **"คู่มือการกรอก
> แบบฟอร์มและตัวอย่างใบเสร็จโครงการวิจัย"** (แบบฟอร์ม วจ.1–วจ.9) ของโปรเจกต์เดิม — **AI ที่สร้างระบบต้อง
> ใส่ seed data ชุดนี้ตรง ๆ ห้ามเดา/แต่งตัวเลขหรือชื่อเอกสารใหม่เอง** (ยกเว้น `budgetItems` ซึ่งเป็นของ
> ผู้ใช้กรอกเองตาม FS-01b — seed แค่ตัวอย่าง 1-2 แถวไว้ทดสอบพอ)

**fundSources (1 แหล่งทุน):**
```
{ id: "fundsource002", fundSourceName: "ทุนภายในมหาวิทยาลัย ปี 2569", fundSourceCode: "MFU-2569" }
```
```
ruleVersions: { id: "ruleversion002", fundSourceId: "fundsource002", versionLabel: "2570.1", isActive: true }
```

**categories (23 หมวด — หมวดสุดท้ายตั้งใจไม่มี `ruleItem` คู่กัน เพราะคู่มือระบุว่ามหาวิทยาลัยไม่สนับสนุน):**
ค่าตอบแทนนักศึกษาช่วยงาน (วิชาการ) · ค่าตอบแทนนักศึกษาช่วยงาน (ทั่วไป) · ค่าตอบแทนอาสาสมัคร/ผู้ให้ข้อมูล ·
ค่าตอบแทนผู้ทรงคุณวุฒิพิจารณาเครื่องมือ · ค่าตอบแทนผู้ทรงคุณวุฒิ (ตรวจทาน Manuscript) · ค่าจ้างเหมาบริการ ·
ค่าธรรมเนียมห้องปฏิบัติการ/เครื่องมือ · ค่าอาหารกลางวัน (ประชุม/อบรม) · ค่าอาหารว่างและเครื่องดื่ม
(ประชุม/อบรม) · ค่าเช่ายานพาหนะ (รถตู้) · ค่าเบี้ยเลี้ยงเดินทาง · ค่าที่พัก (เดินทางปฏิบัติงาน) ·
ค่าเครื่องบิน (ชั้นประหยัด) · ค่าแท็กซี่ (เดินทางปฏิบัติงาน) · ค่าพาหนะส่วนตัว (ต่อกิโลเมตร) ·
ค่าน้ำมันเชื้อเพลิงพาหนะเช่า (ในจังหวัด) · ค่าน้ำมันเชื้อเพลิงพาหนะเช่า (ต่างจังหวัด) · ค่าวัสดุคอมพิวเตอร์ ·
ค่าวัสดุสำนักงาน · ค่าวัสดุอื่นๆ · ค่าสาธารณูปโภค · ค่าครุภัณฑ์ · **ค่าตอบแทนคณะผู้วิจัย** (หมวดสุดท้าย
— ไม่มี `ruleItem`, ทุกใบที่เลือกหมวดนี้ต้องได้ `ไม่เข้าเงื่อนไข` เสมอ)

**ruleItems (22 รายการ, ทั้งหมด `ruleVersionId: "ruleversion002"`):**

| categoryName | rateType | rateAmount | unit | evidenceChainId | note (สาระสำคัญจากคู่มือฯ 2570) |
|---|---|---|---|---|---|
| ค่าตอบแทนนักศึกษาช่วยงาน (วิชาการ) | ต่อหน่วย | 60 | ชั่วโมง | chain-research-assistant | จ้างได้ไม่เกิน 25% ของงบประมาณทั้งโครงการ |
| ค่าตอบแทนนักศึกษาช่วยงาน (ทั่วไป) | ต่อหน่วย | 30 | ชั่วโมง | chain-research-assistant | จ้างได้ไม่เกิน 25% ของงบประมาณทั้งโครงการ |
| ค่าตอบแทนอาสาสมัคร/ผู้ให้ข้อมูล | เพดานตามจริง | 100 | คน/ครั้ง | chain-honorarium | ระดมสมอง/สัมภาษณ์/แบบสอบถาม — เสี่ยงสุขภาพให้เป็นดุลยพินิจกรรมการ |
| ค่าตอบแทนผู้ทรงคุณวุฒิพิจารณาเครื่องมือ | เพดานตามจริง | 2000 | คน | chain-honorarium | พิจารณาตามความจำเป็นเหมาะสม |
| ค่าตอบแทนผู้ทรงคุณวุฒิ (ตรวจทาน Manuscript) | ตามข้อเสนอโครงการ | null | – | chain-honorarium | ไม่เกิน 10% ของงบโครงการ ต้องแนบ CV ผู้ทรงคุณวุฒิ |
| ค่าจ้างเหมาบริการ | ตามข้อเสนอโครงการ | null | – | chain-purchase | ไม่เกิน 25% ของงบหมวดค่าใช้สอย ต้องแนบ TOR |
| ค่าธรรมเนียมห้องปฏิบัติการ/เครื่องมือ | ตามข้อเสนอโครงการ | null | – | chain-purchase | ตามอัตราหน่วยงานผู้ให้บริการ (ในและนอกมหาวิทยาลัย) |
| ค่าอาหารกลางวัน (ประชุม/อบรม) | เพดานตามจริง | 150 | คน/มื้อ | chain-meeting-refreshment | เฉพาะกิจกรรมประชุม/อบรม/Focus group ที่เกี่ยวกับโครงการ |
| ค่าอาหารว่างและเครื่องดื่ม (ประชุม/อบรม) | เพดานตามจริง | 50 | คน/มื้อ | chain-meeting-refreshment | เช่นเดียวกับข้างต้น |
| ค่าเช่ายานพาหนะ (รถตู้) | เหมาจ่าย | 2000 | คัน/วัน | chain-purchase | ยานพาหนะอื่นนอกจากรถตู้ตามข้อตกลงผู้ให้บริการ |
| ค่าเบี้ยเลี้ยงเดินทาง | เหมาจ่าย | 350 | วัน | chain-travel-domestic | อ้างอิงระเบียบเดินทางฉบับที่ 8/2566 — เฉพาะผู้เดินทางจริง |
| ค่าที่พัก (เดินทางปฏิบัติงาน) | เพดานตามจริง | 1800 | วัน | chain-travel-domestic | หรือเลือกเหมาจ่าย 800 บาท/วันแทนได้ (เลือกวิธีใดวิธีหนึ่ง) |
| ค่าเครื่องบิน (ชั้นประหยัด) | เพดานตามจริง | 5500 | เที่ยวไป-กลับ | chain-travel-domestic | ไม่เกินชั้นประหยัด |
| ค่าแท็กซี่ (เดินทางปฏิบัติงาน) | เหมาจ่าย | 700 | คน | chain-travel-domestic | รวมไป-กลับ เฉพาะที่พัก/สถานที่ปฏิบัติงาน↔สถานียานพาหนะ |
| ค่าพาหนะส่วนตัว (ต่อกิโลเมตร) | ต่อหน่วย | 4 | กม. | chain-travel-domestic | ประมาณระยะทางจาก DOH to Travel/Google Map เฉพาะหัวหน้าโครงการ/ผู้ร่วมวิจัยที่เดินทาง |
| ค่าน้ำมันเชื้อเพลิงพาหนะเช่า (ในจังหวัด) | เหมาจ่าย | 1500 | คัน/วัน | chain-purchase | อัตราตั้งงบเท่านั้น เบิกจริงตามใบเสร็จ |
| ค่าน้ำมันเชื้อเพลิงพาหนะเช่า (ต่างจังหวัด) | เหมาจ่าย | 2000 | คัน/วัน | chain-purchase | เช่นเดียวกับข้างต้น |
| ค่าวัสดุคอมพิวเตอร์ | เพดานตามจริง | 5000 | – | chain-purchase | มูลค่าต่อหน่วย/ชุดไม่เกิน 20,000 บาท จึงเป็นวัสดุไม่ใช่ครุภัณฑ์ |
| ค่าวัสดุสำนักงาน | เพดานตามจริง | 5000 | – | chain-purchase | – |
| ค่าวัสดุอื่นๆ | ตามข้อเสนอโครงการ | null | – | chain-purchase | วัสดุวิทยาศาสตร์/ไฟฟ้า/เกษตร/งานบ้าน/เวชภัณฑ์ — ดุลยพินิจกรรมการ |
| ค่าสาธารณูปโภค | เพดานตามจริง | 3000 | – | chain-purchase | ไปรษณีย์/ขนส่ง/โทรศัพท์/เน็ต รวมกันไม่เกิน 3,000 บาท |
| ค่าครุภัณฑ์ | ตามข้อเสนอโครงการ | null | – | chain-purchase | ไม่เกิน 25% ของงบโครงการ กรรมสิทธิ์เป็นของมหาวิทยาลัยเมื่อจบโครงการ |

**documentTypes (16 ชนิด, ใช้ซ้ำข้ามหมวด/ข้ามแหล่งทุนได้):**

| id | ชื่อเอกสาร (formCode) | ใช้เมื่อ |
|---|---|---|
| `voucher-vj1` | ใบสำคัญรับเงิน (วจ.1) | ผู้รับเงินออกใบเสร็จเองไม่ได้ (บุคคลทั่วไป) |
| `payment-cert-vj2` | ใบรับรองการจ่ายเงิน (วจ.2) | เรียกใบเสร็จจากผู้รับเงินไม่ได้เลย (เช่น ค่าแท็กซี่) หัวหน้าโครงการรับรองเอง |
| `travel-approval-vj3` | แบบขออนุมัติเดินทางฯ (วจ.3) | ต้องอนุมัติ**ก่อน**เดินทางเสมอ — ไม่มีใบนี้เบิกค่าเดินทางไม่ได้เลย |
| `hr-leave-form` | แบบใบลา/ขอหยุดงาน | คู่กับ วจ.3/วจ.4 พิมพ์จากระบบ HR มหาวิทยาลัย |
| `travel-report-vj4` | แบบรายงานการเดินทางฯ (วจ.4) | ทำ**หลัง**เดินทางกลับ อ้างอิงเลขที่ วจ.3 |
| `id-card-copy` | สำเนาบัตรประชาชนผู้รับเงิน | ทุกครั้งที่จ่ายเงินสดให้บุคคล (คู่กับ วจ.1/วจ.2/วจ.5/วจ.6/วจ.7/วจ.8) |
| `vehicle-registration-copy` | สำเนาทะเบียนรถ | เบิกค่าน้ำมัน/ระยะทางด้วยรถยนต์ส่วนตัวเท่านั้น |
| `receipt-return-vj5` | ใบสำคัญรับ (คืนเงิน) (วจ.5) | คืนเงินยืมทดรองที่เบิกเกินจริง |
| `honorarium-voucher-vj6` | ใบสำคัญรับเงินสำหรับบุคคลภายนอก (วจ.6) | อาสาสมัคร/ผู้ทรงคุณวุฒิ/วิทยากร |
| `timesheet-fulltime-vj7` | แบบลงเวลาผู้ช่วยวิจัย Full Time (วจ.7) | ผู้ช่วยวิจัยเต็มเวลารายเดือน |
| `timesheet-parttime-vj8` | แบบลงเวลาผู้ช่วยวิจัย นักศึกษาช่วยงาน (วจ.8) | นักศึกษาช่วยงาน (part-time) 1 เดือน/ฉบับ |
| `petty-cash-ledger-vj9` | ทะเบียนคุมเงินสดย่อย (วจ.9) | โครงการที่ตั้งวงเงินสดย่อย — **ไม่ผูกกับ evidence chain ใดเลย** (ไม่บังคับ) |
| `tax-invoice-full` | ใบกำกับภาษีเต็มรูปแบบ/ใบเสร็จรับเงิน | ร้านค้า/บริษัทจดทะเบียน VAT |
| `tax-invoice-abbreviated` | ใบกำกับภาษีอย่างย่อ | เช่น ใบเสร็จค่าไปรษณีย์ |
| `procurement-committee-cert` | ใบตรวจรับพัสดุ (กรรมการ 3 คน) | **บังคับเฉพาะซื้อ/จ้างเกิน 100,000 บาท** แทนลายเซ็นหัวหน้าโครงการคนเดียว |
| `attendee-signin-sheet` | รายชื่อผู้เข้าร่วมกิจกรรม+ลายเซ็น | คู่กับค่าอาหารว่าง/ค่าตอบแทนวิทยากรกิจกรรมกลุ่ม |

> แต่ละ `documentType` ยังมี `requiredFields[]`/`validityChecks[]`/`sourceExcerpt` ละเอียดกว่านี้ (เช่น
> `voucher-vj1` ต้องมี "เลขที่ข้อตกลง/สัญญา", "ลายเซ็นผู้รับเงิน" ฯลฯ, ตรวจ "บัตรประชาชนต้องไม่หมดอายุ")
> — **ยังไม่ต้องเก็บ field พวกนี้ในระบบตอนนี้** (ดูหัวข้อ 8 ข้อใหม่: ตรวจแค่ "แนบไฟล์ครบชนิดไหม" ไม่ตรวจ
> เนื้อหาในไฟล์) แต่เก็บ `sourceExcerpt` สั้น ๆ ไว้เป็น note ในหน้าจอได้ถ้าต้องการ

**evidenceChains (5 ชุด — `steps[]` ที่ `required: true` ทุกขั้นต้องมีไฟล์ตรงชนิดตาม FS-03 ชั้นที่ 3):**

- **`chain-travel-domestic`** (เดินทางในประเทศ):
  1. **[บังคับ]** ก่อนเดินทาง: `travel-approval-vj3`
  2. **[บังคับ]** ก่อนเดินทาง: `hr-leave-form`
  3. **[บังคับ]** หลังเดินทาง: `travel-report-vj4`
  4. **[บังคับ]** หลักฐานจ่ายจริง: `tax-invoice-full` หรือ `voucher-vj1` หรือ `payment-cert-vj2` (อย่างใดอย่างหนึ่ง)
  5. *[ไม่บังคับ]* `id-card-copy` — เฉพาะจ่ายเงินสดให้บุคคลด้วย วจ.1/วจ.2
  6. *[ไม่บังคับ]* `vehicle-registration-copy` — เฉพาะเบิกค่าน้ำมัน/ระยะทางด้วยรถส่วนตัว
- **`chain-honorarium`** (ค่าตอบแทนบุคคลภายนอก):
  1. **[บังคับ]** `honorarium-voucher-vj6` หรือ `voucher-vj1`
  2. **[บังคับ]** `id-card-copy`
  3. *[ไม่บังคับ]* `attendee-signin-sheet` — เฉพาะกิจกรรมกลุ่ม
- **`chain-research-assistant`** (ค่าตอบแทนผู้ช่วยวิจัย/นักศึกษาช่วยงาน):
  1. **[บังคับ]** `timesheet-fulltime-vj7` หรือ `timesheet-parttime-vj8` (ตามประเภทการจ้าง)
  2. **[บังคับ]** `voucher-vj1`
  3. **[บังคับ]** `id-card-copy`
- **`chain-purchase`** (ซื้อวัสดุ/จ้างเหมา/ครุภัณฑ์/สาธารณูปโภค):
  1. **[บังคับ]** `tax-invoice-full` หรือ `tax-invoice-abbreviated`
  - **conditionalStep:** ถ้า `amount > 100000` → เพิ่มขั้นตอนบังคับ `procurement-committee-cert`
- **`chain-meeting-refreshment`** (ค่าอาหารว่าง/อาหาร กิจกรรมกลุ่ม):
  1. **[บังคับ]** `tax-invoice-full` หรือ `voucher-vj1`
  2. *[ไม่บังคับ]* `id-card-copy` — เฉพาะกรณีใช้ วจ.1
  3. **[บังคับ]** `attendee-signin-sheet`

---

## 4. หน้าจอ (screens)

1. `login.html` / `signup.html` — ล็อกอิน/สมัครสมาชิก (ไม่มี nav ทั้งเว็บ)
2. `index.html` — หน้าแรก อธิบายระบบ + คำอธิบายสถานะ
3. `projects.html` — รายการ/สร้างโครงการวิจัยของนักวิจัย (เลือกแหล่งทุนจาก dropdown) — สร้างเสร็จแล้ว
   พาไปหน้า `project-budget.html` ทันทีเพื่อกรอกงบประมาณ (ดู FS-01b)
4. `project-budget.html` (**ใหม่**) — กรอก/แก้/ลบรายการประมาณการค่าใช้จ่ายของโครงการนั้น (บังคับต้องมี
   อย่างน้อย 1 รายการก่อนอัปโหลดใบเสร็จได้ — ดู FS-01b)
5. `new-receipt.html` — อัปโหลดใบเสร็จ 2 ขั้นตอน: (1) เลือกไฟล์ + ยินยอมความเป็นส่วนตัวครั้งแรก +
   เชื่อมต่อ Google Drive (2) ตรวจ/แก้ข้อมูลที่ AI อ่านมา แล้วส่งตรวจ — ถ้าโครงการที่เลือกยังไม่มี
   `budgetItems` เลย ต้องเตือนและพาไปกรอกงบประมาณก่อน (ดู FS-01b)
6. `receipts.html` — รายการใบเสร็จของนักวิจัย (กรองตามโครงการได้ผ่าน `?projectId=`), ปุ่มลบต่อรายการ
7. `fund-sources.html` — **Admin only**: เพิ่ม/ลบแหล่งทุน, ตารางแก้ไข `ruleItems` ต่อแหล่งทุน
8. `admin-dashboard.html` — **Admin only**: สรุปสถิติรวม + ตารางใบเสร็จทุกใบของทุกคนพร้อมชื่อ/อีเมลเจ้าของ

---

## 5. Functional flows / User Stories (เกณฑ์การยอมรับ)

### FS-01 — สร้างโครงการวิจัย
**ในฐานะ**นักวิจัย **ฉันต้องการ**สร้างโครงการของตัวเอง **เพื่อ**ผูกใบเสร็จเข้ากับแหล่งทุนที่ถูกต้อง
- [ ] ฟอร์มมีชื่อโครงการ + เลือกแหล่งทุนจาก dropdown (อ่านจาก `fundSources` จริง)
- [ ] สร้างแล้วพาไปหน้า `project-budget.html` ของโครงการนั้นทันที (ยังไม่ถือว่าใช้งานโครงการได้เต็มที่
  จนกว่าจะกรอกงบประมาณตาม FS-01b) การ์ดโครงการใน `projects.html` แสดงทันทีหลังสร้าง พร้อมลิงก์
  "ใบเสร็จในโครงการนี้", "อัปโหลดใบเสร็จ" และ "งบประมาณโครงการ"

### FS-01b — อัปโหลด/กรอกรายการประมาณการค่าใช้จ่ายของโครงการ (**ใหม่**, บังคับก่อนอัปโหลดใบเสร็จ)
**ในฐานะ**นักวิจัย **ฉันต้องการ**บันทึกรายการประมาณการค่าใช้จ่ายของโครงการไว้ล่วงหน้า **เพื่อ**ให้ระบบ
เทียบใบเสร็จที่ส่งเข้ามากับตัวเลขที่โครงการนี้ของบไว้จริง ไม่ใช่แค่เพดานกลางของแหล่งทุน
- [ ] กรอกได้ 2 ทาง: **(ก)** กรอกมือทีละแถวในตาราง (หมวด/จำนวนเงินประมาณการ/หน่วย/หมายเหตุ) หรือ
  **(ข)** แนบไฟล์รูป/PDF ของประมาณการ แล้วให้ AI (โมเดล/วิธีเดียวกับ FS-02) อ่านแล้วเสนอรายการมาให้ —
  **นักวิจัยต้องตรวจ/แก้ก่อนกดบันทึกเสมอ** (AI แค่เสนอ ไม่บันทึกตรงให้)
- [ ] ชื่อหมวดควรอิงตามรายการหมวดของแหล่งทุนที่โครงการนี้ผูกอยู่ (ให้เลือกจาก dropdown เดียวกับที่
  `new-receipt.html` ใช้) เพื่อให้จับคู่ตอนตรวจใบเสร็จได้ตรง ๆ ด้วยชื่อหมวด
  ไม่ต้องทำ fuzzy-match ข้ามชื่อ
- [ ] แก้ไข/ลบรายการที่บันทึกไปแล้วได้ตลอด (ไม่ใช่ตั้งครั้งเดียวแล้วล็อก)
- [ ] **โครงการที่ยังไม่มี `budgetItems` เลยแม้แต่รายการเดียว ห้ามอัปโหลดใบเสร็จเข้าโครงการนั้น** —
  `new-receipt.html` ต้องเช็คก่อนและเตือน/พาไปหน้านี้ก่อน ไม่ใช่ปล่อยให้ส่งแล้วค่อยพังทีหลัง

### FS-02 — อัปโหลดใบเสร็จ + AI อ่านข้อมูล
**ในฐานะ**นักวิจัย **ฉันต้องการ**แนบรูป/PDF ใบเสร็จแล้วให้ AI กรอกข้อมูลให้ **เพื่อ**ไม่ต้องพิมพ์เอง
- [ ] แนบไฟล์ได้สูงสุด 5 ไฟล์/ใบเสร็จ (jpg/png/pdf)
- [ ] ถ้ามีไฟล์แนบ AI (OpenRouter, `google/gemini-2.5-flash-lite`) อ่านผู้ขาย/วันที่/จำนวนเงิน/หมวด
  จากรูปจริง (PDF แปลงหน้าแรกเป็นรูปก่อนส่งให้ AI) และมีป้าย "🤖 AI suggestion" ให้เห็นว่าเป็นผลจาก AI
  จริง ไม่ใช่ค่าสุ่ม
- [ ] ถ้าไม่มีคีย์ AI หรือเรียก AI ไม่สำเร็จ ให้ตกไปใช้ข้อมูลตัวอย่าง (mock) แทน **ห้ามระบบล้ม**
- [ ] ต้องติ๊กยินยอมความเป็นส่วนตัวก่อนอัปโหลดครั้งแรก (บอกว่า Admin เห็นข้อมูลได้เต็มรูปแบบ)
- [ ] ถ้าแนบไฟล์แต่ยังไม่เชื่อมต่อ Google Drive ต้องเตือนและบล็อกการส่ง ไม่ใช่ส่งแบบเงียบ ๆ
- [ ] **แต่ละไฟล์ที่แนบ ต้องระบุว่าเป็น "เอกสารประเภทไหน" (`documentTypeId`)** — dropdown ตัวเลือกจำกัด
  เฉพาะ `documentTypeIds` ที่ปรากฏใน `steps`/`conditionalSteps` ของ evidence chain ที่ผูกกับหมวด
  ที่เลือกไว้เท่านั้น (ไม่ใช่ทั้ง 16+ ชนิดในระบบ) — AI ช่วยเดาประเภทให้จากภาพไฟล์นั้น (เรียกโมเดลเดียวกับ
  ที่อ่านใบเสร็จ) เป็นค่าเริ่มต้นในดรอปดาวน์ แต่**นักวิจัยต้องยืนยัน/แก้ก่อนส่งเสมอ** (AI แค่เสนอ ไม่ตัดสิน
  ว่าครบหรือไม่ครบ — นั่นเป็นหน้าที่ของ Rule Engine ใน FS-03 ชั้นที่ 3) ถ้าหมวดที่เลือกไม่มี evidence
  chain ผูกอยู่เลย ให้ข้ามขั้นนี้ (ไม่มีตัวเลือกให้เลือก)
- [ ] หลังเลือก `documentTypeId` ต่อไฟล์แล้ว ให้เข้าสู่ขั้นตอนดึง+ตรวจข้อความในเอกสาร (**FS-08**) ก่อน
  ปุ่มส่งตรวจจะกดได้

### FS-03 — ตรวจใบเสร็จ 4 ชั้น: กฎแหล่งทุน + งบประมาณโครงการ + ความครบถ้วนเอกสาร + เนื้อหาเอกสาร (Rule Engine กำหนดทั้งหมด, ไม่ใช่ AI)
**ในฐานะ**ระบบ **ต้องการ**ตัดสินสถานะแบบที่ตรวจสอบซ้ำได้เสมอ (deterministic)

**ชั้นที่ 1 — เพดานของแหล่งทุน (เดิม, ไม่เปลี่ยน):**
- [ ] ค้นหา `ruleItem` ที่ตรงกับหมวดที่เลือก ในแหล่งทุนของโครงการนั้น
- [ ] `เพดานตามจริง`/`เหมาจ่าย`: เทียบจำนวนเงินกับ `rateAmount` ตรง ๆ → ผ่าน/ต้องแก้ไข
- [ ] `ต่อหน่วย`/`ตามข้อเสนอโครงการ`: ไม่มีเพดานระดับแหล่งทุนให้ตรวจ → ส่งต่อให้ชั้นที่ 2 ตัดสินแทน
- [ ] หมวดที่ไม่มี `ruleItem` ตรงกันเลย → `ไม่เข้าเงื่อนไข` (จบที่ชั้นนี้ ไม่ต้องเช็คชั้น 2 ต่อ)

**ชั้นที่ 2 — เทียบกับงบประมาณที่โครงการนี้ประมาณการไว้เอง (`budgetItems`, ใหม่):**
- [ ] ค้นหา `budgetItem` ของ**โครงการนี้**ที่ `categoryName` ตรงกับหมวดของใบเสร็จ
- [ ] ถ้า**ไม่มี** `budgetItem` ตรงกันเลย (โครงการนี้ไม่ได้ของบหมวดนี้ไว้) → สถานะ `ไม่เข้าเงื่อนไข` แม้ชั้น 1
  จะผ่านก็ตาม (ของบไว้ก่อนถึงจะเบิกได้ — ไม่ใช่แค่หมวดมีอยู่ในระบบแหล่งทุน)
- [ ] ถ้ามี `budgetItem` ตรงกัน: รวมยอดใบเสร็จ**ที่เคยผ่านแล้ว**ของโครงการนี้ในหมวดเดียวกัน (ไม่รวมใบที่ยัง
  ไม่ผ่าน/ถูกปฏิเสธ) **บวก**จำนวนเงินของใบเสร็จใบนี้ แล้วเทียบกับ `estimatedAmount` ของ `budgetItem` นั้น
  - เกินยอดประมาณการ → `ต้องแก้ไข` (แม้ชั้น 1 จะผ่านก็ตาม)
  - ไม่เกิน → ผ่านชั้นนี้ (สถานะสุดท้าย = ผลจากชั้น 1 ถ้าชั้น 1 ตัดสินไปแล้ว, หรือ `ผ่าน` ถ้าชั้น 1
    ส่งต่อมาให้ชั้น 2 ตัดสิน)
**ชั้นที่ 3 — ความครบถ้วนของเอกสารแนบตาม evidence chain (ใหม่):**
- [ ] ถ้า `ruleItem` ที่จับคู่ได้ (จากชั้น 1) ไม่มี `evidenceChainId` เลย → ข้ามชั้นนี้ (ไม่มีอะไรให้เช็ค)
- [ ] ถ้ามี: ดึง `evidenceChain.steps[]` ที่ `required == true` ทั้งหมด บวก `conditionalSteps[]` ที่เงื่อนไข
  ตรงกับใบเสร็จนี้จริง (เช่น `amount > 100000`) มารวมเป็นรายการ "ขั้นตอนที่บังคับ" ของใบเสร็จใบนี้
- [ ] แต่ละขั้นตอนบังคับ ต้องมี**ไฟล์แนบอย่างน้อย 1 ไฟล์**ที่ `documentTypeId` อยู่ใน `documentTypeIds[]`
  ของขั้นตอนนั้น (ขั้นตอนที่ยอมรับได้หลายชนิดแบบ "อย่างใดอย่างหนึ่งก็พอ" ให้ถือว่าผ่านถ้ามีไฟล์ตรงชนิด
  ใดชนิดหนึ่งในนั้น)
- [ ] มีขั้นตอนบังคับใดขั้นตอนหนึ่งที่ไม่มีไฟล์ตรงชนิดเลย → `ต้องแก้ไข` (แม้ชั้น 1/ชั้น 2 จะผ่านก็ตาม) —
  พร้อมรายชื่อ `documentType` ที่ขาดแนบไว้ให้ Rule Engine ส่งต่อให้ FS-04 อธิบาย
- [ ] ครบทุกขั้นตอนบังคับ → ผ่านชั้นนี้ (สถานะสุดท้าย = ผลจากชั้น 1/2 เดิม)
**ชั้นที่ 4 — เนื้อหาในเอกสารแนบตรงตาม `requiredFields`/`validityChecks` ไหม (ใหม่, ดู FS-08 สำหรับรายละเอียดเต็ม):**
- [ ] ใช้ `extractedFields` ของแต่ละไฟล์ (ค่าที่นักวิจัย**ยืนยัน/แก้แล้ว** ตาม FS-08 ไม่ใช่ค่าดิบจาก AI)
- [ ] field ใน `requiredFields[]` ที่ไม่มีค่า (ว่าง/ไม่ได้กรอก) → ถือว่าขาด field นั้น
- [ ] `validityChecks[]` ที่เทียบเป็นค่าได้ตรง ๆ (วันที่/ตัวเลข/ข้อความระหว่าง 2 เอกสาร) → โค้ดเทียบค่าเอง
- [ ] `validityChecks[]` ที่เป็นการเทียบด้วยสายตา (เช่น ลายเซ็นเหมือนกันไหม) → ทำอัตโนมัติไม่ได้ ให้ขึ้นเป็น
  "ควรตรวจเพิ่มเติมด้วยคน" แนบไปกับคำอธิบาย **ไม่นับเป็นเหตุให้ตกสถานะ**
- [ ] field ขาด หรือ validityCheck ที่เทียบได้แล้วไม่ผ่าน → `ต้องแก้ไข` (แม้ชั้น 1-3 จะผ่านก็ตาม)

- [ ] เก็บผลของทั้ง 4 ชั้นแยกกันไว้ใน `aiExplanation`/log (ไม่ใช่ทับกันจนบอกไม่ได้ว่าใบนี้ตกเพราะชั้นไหน)
- [ ] **ผลลัพธ์ทั้ง 4 ชั้นห้าม AI เป็นคนตัดสิน** — ต้องเป็นโค้ดเทียบค่า/รายการล้วน ๆ (AI แค่เดาว่าไฟล์
  ไหนเป็นเอกสารประเภทไหนตอนอัปโหลด ตาม FS-02, และแค่ดึงข้อความออกมาตาม FS-08 — ไม่ใช่คนตัดสินว่า
  ครบ/ถูกต้องหรือไม่)

### FS-04 — คำอธิบายผลตรวจจาก AI (agentic, อ้างอิงกฎที่มีอยู่จริงเท่านั้น)
**ในฐานะ**นักวิจัย **ฉันต้องการ**คำอธิบายภาษาที่เข้าใจง่าย **เพื่อ**รู้ว่าต้องแก้อะไร/แนบเอกสารอะไรเพิ่ม
- [ ] ระบบดึง `evidenceChain` + `documentTypes` ที่ผูกกับหมวดนั้นจาก Firestore ก่อน
- [ ] ส่งเฉพาะ: สถานะ + จำนวนเงิน + หมวด + รายการเอกสารที่กฎกำหนดไว้จริง + **ผลชั้นที่ 1 (เพดานแหล่งทุน),
  ชั้นที่ 2 (ยอดสะสมเทียบ `budgetItem.estimatedAmount` ของโครงการนี้ ถ้ามี), ชั้นที่ 3 (รายชื่อ
  `documentType` ที่ยังขาดแนบ ถ้ามี), ชั้นที่ 4 (field/validityCheck ที่ไม่ผ่านในเอกสารที่แนบมาแล้ว
  ถ้ามี)** ให้ AI สรุปเป็นคำอธิบาย
- [ ] ถ้าใบเสร็จตกเพราะชั้น 2 (เกินงบที่โครงการของบไว้) คำอธิบายต้องบอกตัวเลขจริง: ของบไว้เท่าไร
  ใช้ไปแล้วเท่าไร (รวมใบนี้) เกินไปเท่าไร — ไม่ใช่แค่บอกว่า "เกินงบ" ลอย ๆ
- [ ] ถ้าใบเสร็จตกเพราะชั้น 3 (เอกสารแนบไม่ครบ) คำอธิบายต้องระบุ**ชื่อเอกสารที่ยังขาด**ทีละรายการ
  (จาก `documentType.documentName`/`formCode`) ไม่ใช่บอกลอย ๆ ว่า "เอกสารไม่ครบ"
- [ ] ถ้าใบเสร็จตกเพราะชั้น 4 (เนื้อหาเอกสารไม่ครบ/ไม่ผ่าน) คำอธิบายต้องระบุ**ชื่อเอกสาร + ชื่อ field ที่
  ขาด/ไม่ผ่าน**ทีละรายการ (เช่น "วจ.1: ไม่พบลายเซ็นหัวหน้าโครงการ") และถ้ามีรายการ "ควรตรวจเพิ่มเติมด้วย
  คน" ให้แสดงแยกจากรายการที่ทำให้ตกสถานะจริง ๆ ชัดเจน ไม่ปนกัน
- [ ] AI **ห้ามอ้างอิงความรู้ภายนอก ห้ามเดา/แต่งกฎหรือตัวเลขงบประมาณที่ไม่มีอยู่จริงใน Firestore** และ
  **ห้ามอ้างว่าตรวจไฟล์แนบจริงแล้ว**
  (AI ไม่ได้รับไฟล์แนบ บอกได้แค่ "กฎกำหนดให้ต้องมีเอกสารอะไรบ้าง" ไม่ใช่ "ไฟล์นี้ถูกต้องแล้ว")
- [ ] ทุกครั้งที่ประมวลผล (ใช้ AI สำเร็จ หรือ fallback) ต้องบันทึก log ลง `agentLogs` subcollection
  (ใช้ AI จริงหรือไม่, โมเดล, ใช้เวลาเท่าไร) — ถือเป็น audit trail
- [ ] ถ้า AI เรียกไม่สำเร็จ ให้ fallback เป็นรายการเอกสารที่ต้องแนบ (จาก Firestore ล้วน ๆ ไม่ต้องมี AI)
  ต่อท้ายคำอธิบายจาก Rule Engine เดิม

### FS-05 — ดูรายการใบเสร็จของตัวเอง + ลบ
- [ ] แสดงใบเสร็จของนักวิจัยคนนั้นทั้งหมด (ทุกโครงการ) เรียงตามเวลาอัปโหลดล่าสุดก่อน
- [ ] กรองตามโครงการได้ผ่าน `?projectId=`
- [ ] ปุ่มลบต่อใบเสร็จ ลบเฉพาะของตัวเอง (cascade ลบ `files` subcollection ด้วย)

### FS-06 — Admin: จัดการแหล่งทุน/กฎ
- [ ] เพิ่ม/ลบแหล่งทุน (ลบ cascade ลบ `ruleVersions`/`ruleItems` ของแหล่งทุนนั้นด้วย)
- [ ] ตารางแก้ไข `ruleItems` ต่อแหล่งทุน: หมวด/ประเภทอัตรา/จำนวนเงิน/หน่วย/เอกสารที่ต้องใช้/หมายเหตุ/
  เลือก evidence chain จาก dropdown
- [ ] ผู้ใช้ที่ไม่ใช่ admin เข้าหน้านี้ตรง ๆ ผ่าน URL ต้องเห็น banner "ไม่มีสิทธิ์" ไม่ใช่ฟอร์มจริง (แต่
  การกันสิทธิ์จริงต้องอยู่ที่ Firestore rules ไม่ใช่แค่ซ่อนฟอร์ม)

### FS-07 — Admin Dashboard
- [ ] อ่าน `collectionGroup("receipts")` **ไม่มี** `.where(ownerUserId,...)` (เพราะต้องเห็นของทุกคน)
- [ ] สรุปสถิติ: รวมทั้งหมด, แยกตามสถานะ/หมวด/แหล่งทุน
- [ ] ตารางใบเสร็จทุกใบพร้อมชื่อ-อีเมลเจ้าของ (resolve จาก `ownerUserId` → `users/{uid}`)

### FS-08 — AI ดึงข้อความจากเอกสารแนบ + นักวิจัยตรวจ/แก้ ก่อนนำไปเทียบกับ Rule Engine (ชั้นที่ 4 ของ FS-03)
**ในฐานะ**นักวิจัย **ฉันต้องการ**ให้ระบบช่วยดึงข้อมูลจากเอกสารที่แนบมาให้ **เพื่อ**ไม่ต้องพิมพ์ทวนเอง
แต่ยังคงเป็นคนยืนยันความถูกต้องก่อนที่ระบบจะเอาไปตัดสินผล

**ขั้นตอน (ต่อจาก FS-02 — เกิดหลังเลือก `documentTypeId` ให้ไฟล์นั้นแล้ว):**
- [ ] ถ้าไฟล์นั้นมี `documentTypeId` ที่ `requiredFields[]` ไม่ว่าง: เรียก AI (โมเดล/วิธีเดียวกับ FS-02)
  พร้อมส่งรายการ `requiredFields[]` ของ `documentType` นั้นไปด้วย สั่งให้ **"ดึงข้อความที่เห็นจริงในไฟล์
  มาเป็นคู่ field–value เท่านั้น ห้ามประเมิน/ตัดสินว่าถูกต้องหรือไม่"** — ผลลัพธ์เป็น JSON
  `{ field: value | "ไม่พบ" }` ต่อไฟล์
- [ ] แสดงผลที่ AI ดึงมาเป็น**ฟอร์มที่แก้ไขได้**ทีละ field (ไม่ใช่ข้อความอ่านอย่างเดียว) — field ที่ AI
  บอกว่า "ไม่พบ" ให้แสดงเป็นช่องว่างเปล่าให้กรอกเอง ไม่ใช่บล็อกไม่ให้ไปต่อ
- [ ] **นักวิจัยต้องตรวจทุก field แล้วกดยืนยัน (หรือแก้ไขค่าที่ผิด/เติมค่าที่ AI พลาด) ก่อนปุ่มส่งตรวจใบเสร็จ
  จะกดได้** — ค่าที่บันทึกลง `files/{fileId}.extractedFields` คือ**ค่าที่ยืนยัน/แก้แล้ว** ไม่ใช่ค่าดิบ
  จาก AI (`extractedFieldsWasAi` เก็บไว้แค่บอกว่าตอนแรก AI เคยดึงให้หรือไม่ ไม่ใช่ค่าที่เอาไปตรวจจริง)
- [ ] ถ้าไม่มีคีย์ AI หรือเรียก AI ไม่สำเร็จ: แสดงฟอร์มเดิมแต่ **ทุกช่องว่างเปล่าให้กรอกเอง** (ไม่บล็อกการ
  ทำงาน — นักวิจัยกรอกมือแทน AI ได้เสมอ)
- [ ] `documentType` ที่ `requiredFields[]` ว่างเปล่า หรือไฟล์ที่ยังไม่ได้ระบุ `documentTypeId` → ข้าม
  ขั้นตอนนี้ (ไม่มีอะไรให้ดึง/ตรวจ)
- [ ] บันทึก log ลง `agentLogs` ด้วย: ใช้ AI ดึงสำเร็จไหม, นักวิจัยแก้ค่าที่ AI ดึงมาไหม (`wasEdited`
  ต่อ field) — เป็น audit trail แยกจาก log ของ FS-04

**สิ่งที่ FS-08 ไม่ทำ (ขอบเขตชัดเจน):**
- [ ] **ไม่ตัดสินว่า field/เอกสารนั้น "ถูกต้อง" หรือไม่** — แค่ดึงข้อความออกมา การตัดสินครบ/ไม่ครบ/ผ่าน
  `validityChecks` ที่เทียบเป็นค่าได้ เป็นหน้าที่ของ Rule Engine ใน FS-03 ชั้นที่ 4 เท่านั้น
- [ ] **ไม่เทียบด้วยสายตา** (ลายเซ็นเหมือนกันไหม, ตราประทับของจริงไหม) — ไม่มีข้อความให้ดึงสำหรับกรณีนี้
  ยังต้องให้คนตรวจเอง (ดู FS-03 ชั้นที่ 4)

---

## 6. Security rules (ต้อง deploy จริง — บัญชีที่ 2 ต้องเปิดของบัญชีที่ 1 ไม่ได้)

หลักการ: **เจ้าของเท่านั้นเขียนได้, admin อ่านได้ทุกอย่างข้ามผู้ใช้แต่เขียนไม่ได้เลย** ยกเว้น
`fundSources`/`ruleVersions`/`ruleItems`/`documentTypes`/`evidenceChains` ที่ admin เขียนได้ (ข้อมูลกลาง
ให้ทุกคนที่ล็อกอินอ่านได้)

```
function isAdmin() {
  return request.auth != null
    && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.get('isAdmin', false) == true;
}

match /users/{userId} {
  allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
  allow delete: if request.auth != null && request.auth.uid == userId;
  // ป้องกัน self-promote: isAdmin ต้องเท่าเดิมเสมอเมื่อเขียนจาก client
  allow create: if request.auth != null && request.auth.uid == userId
    && request.resource.data.get('isAdmin', false) == false;
  allow update: if request.auth != null && request.auth.uid == userId
    && request.resource.data.get('isAdmin', false) == resource.data.get('isAdmin', false);

  match /projects/{projectId} {
    allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
    allow write: if request.auth != null && request.auth.uid == userId;

    match /budgetItems/{budgetItemId} {
      allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    match /receipts/{receiptId} {
      allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
      allow write: if request.auth != null && request.auth.uid == userId;

      match /files/{fileId} {
        allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
        allow write: if request.auth != null && request.auth.uid == userId;
      }
      match /agentLogs/{logId} {
        allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
        allow write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}

// สำหรับ collection-group query ("ใบเสร็จของฉัน" ข้ามโครงการ, และ Admin Dashboard ข้ามทุกคน)
match /{path=**}/receipts/{receiptId} {
  allow read: if request.auth != null && (resource.data.ownerUserId == request.auth.uid || isAdmin());
  allow write: if request.auth != null && request.resource.data.ownerUserId == request.auth.uid;
}

match /fundSources/{fundSourceId} {
  allow read: if request.auth != null;
  allow write: if isAdmin();
  match /ruleVersions/{ruleVersionId} {
    allow read: if request.auth != null;
    allow write: if isAdmin();
    match /ruleItems/{ruleItemId} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }
  }
}

match /documentTypes/{id} { allow read: if request.auth != null; allow write: if isAdmin(); }
match /evidenceChains/{id} { allow read: if request.auth != null; allow write: if isAdmin(); }

match /{document=**} {
  allow read, write: if false;
}
```

**⚠️ Gotcha ที่ห้ามพลาดซ้ำ:** ห้าม nest `match /files/{fileId}` ไว้ใต้ `match /{path=**}/receipts/{receiptId}`
แบบ wildcard — ต้องประกาศ path เต็มแบบ concrete แยกต่างหาก (ดูตัวอย่างข้างบน) ไม่งั้นอ่าน `files`
subcollection ไม่ได้แม้ path จะถูกต้อง

**ทดสอบก่อนถือว่าผ่าน:** สมัคร 2 บัญชีจริง แต่ละบัญชีต้องเห็นแค่ของตัวเอง และลองอ่าน path ของอีกบัญชี
ตรง ๆ ต้องได้ `permission-denied` (นี่คือเทสต์ความปลอดภัยที่การบ้านนี้บังคับให้มี)

---

## 7. Mocked vs Real — บอกตรง ๆ ว่าอะไรจริง อะไรจำลอง

| ส่วน | สถานะ |
|---|---|
| OCR อ่านใบเสร็จ | **จริง** — เรียก OpenRouter vision model ตรง ๆ ไม่มี OCR แยกต่างหาก |
| Rule Engine (ตัดสินผ่าน/ไม่ผ่าน) | **จริง** — เทียบตัวเลข/รายการจาก Firestore 3 ชั้น (เพดานแหล่งทุน + งบประมาณที่โครงการของบไว้เอง ผ่าน `budgetItems` + ความครบถ้วนของเอกสารแนบตาม evidence chain) |
| คำอธิบายผลจาก LLM | **จริง** — เรียก AI จริง พร้อม fallback เมื่อ AI ใช้ไม่ได้ |
| การบังคับแนบเอกสารตาม evidence chain (ชนิดไฟล์) | **จริง** — เช็คว่าทุกขั้นตอนบังคับของ evidence chain มีไฟล์ที่ระบุ `documentTypeId` ตรงชนิดแนบมาจริงหรือไม่ (ชนิดของแต่ละไฟล์ให้ AI เดา นักวิจัยยืนยัน/แก้ก่อนส่ง — ดู FS-02) |
| ตรวจเนื้อหาในเอกสารแนบ (`requiredFields`/`validityChecks`) | **จริงบางส่วน** — AI ดึงข้อความออกมา นักวิจัยตรวจ/แก้ก่อน (FS-08) แล้ว Rule Engine เทียบค่าที่ยืนยันแล้วแบบโค้ดล้วน ๆ (FS-03 ชั้นที่ 4) ครอบคลุมเฉพาะ field ที่มีข้อความให้ดึง/เทียบได้ — **`validityChecks` ที่เป็นการเทียบด้วยสายตา (เช่น ลายเซ็นเหมือนกันไหม, ตราประทับของจริงไหม) ยังทำอัตโนมัติไม่ได้** ขึ้นเป็น "ควรตรวจเพิ่มเติมด้วยคน" แทน ไม่กระทบสถานะ |
| ไฟล์แนบ | ใช้ **Google Drive ของผู้ใช้เอง** ไม่ใช่ Firebase Storage |
| PDPA | **เบามาก** — มีแค่ checkbox ยินยอมครั้งแรก + ปุ่มลบใบเสร็จของตัวเอง ไม่มี consent record แยก,
  ไม่มีสิทธิ์ขอสำเนา/ถอนความยินยอมแบบเต็มรูปแบบ |

---

## 8. นอกขอบเขต (Out of scope — อย่าทำถ้าไม่มีคนขอเพิ่ม)

- Role/action ของ "เจ้าหน้าที่การเงิน" และ "ผู้อนุมัติทุน"
- Deadline tracking รายทุน + แจ้งเตือนตามกำหนดเวลา
- Export รายงานใบเสร็จที่ผ่านตรวจแล้ว
- เทียบ**ด้วยสายตา**ว่าลายเซ็น/ตราประทับในเอกสารแนบเป็นของจริง/ตรงกับเอกสารอื่นหรือไม่ (เช่น ลายเซ็น
  ผู้รับเงินใน วจ.1 ต้องเหมือนลายเซ็นในสำเนาบัตรประชาชน) — ไม่มีข้อความให้ AI ดึงมาเทียบได้ ยังต้องให้คน
  ตรวจเอง (ดูหัวข้อ 7 และ FS-08)
- PDPA compliance เต็มรูปแบบ (consent record แยก, versioning, ขอสำเนาข้อมูล, ถอนความยินยอม)
- Version-history ของกฎ (`ruleVersions`) — แก้ในเวอร์ชัน active เดียวพอ

---

## 9. คีย์ AI / ความปลอดภัยของ secret (บังคับตามเกณฑ์การบ้านนี้)

- **ห้ามมีคีย์ AI (เช่น `sk-or-...`) อยู่ในไฟล์ที่ commit เข้า git เด็ดขาด** — เก็บไว้ในไฟล์ local ที่
  `.gitignore` ไว้ (เช่น `js/ai-config.local.js`) และมีไฟล์ตัวอย่าง `js/ai-config.example.js` เป็น
  placeholder แทน
- ก่อน push ทุกครั้งให้เช็คว่าไม่มีคีย์หลุดไปในไฟล์ที่ commit (`git log --all -p | grep sk-or` ต้องไม่เจอ)
- ถ้าจะให้ปุ่ม AI ทำงานได้จริงบนเว็บที่ deploy แล้ว (ให้อาจารย์ทดสอบได้) ให้บันทึกไว้ใน `BACKLOG.md`
  ว่าเป็นข้อจำกัดที่คีย์จะมองเห็นได้จาก dev tools ของผู้เข้าเว็บ (เพราะไม่มี server-side proxy) — ไม่ใช่
  วิธีที่ปลอดภัยสำหรับ production จริง แต่พอสำหรับการบ้านนี้

---

## 10. รายการที่ต้องส่ง (ตาม w9-homework.html) — เช็คก่อนส่ง

- [ ] README.md บรรทัดบนสุด: URL เว็บที่ deploy จริง + ลิงก์ `test-results.md`
- [ ] `.claude/agents/` มีผู้ช่วย **3 ตัว** ที่ใช้โมเดลต่างกัน (เสนอแผนให้คนตรวจก่อนสร้างจริง — ดูหัวข้อ 0.1)
- [ ] ชุดทดสอบ **5 ตัว** อย่างน้อย 1 ตัวเป็นเทสต์ความปลอดภัย (บัญชี A เปิดของบัญชี B ไม่ได้ — ดูหัวข้อ 6)
- [ ] `test-results.md` สรุปผลทดสอบ — แยกให้ชัดว่า "โค้ดผิด" หรือ "เทสต์เขียนผิด" **ห้ามแก้โค้ดแค่ให้เทสต์ผ่าน**
- [ ] `BACKLOG.md` ระบุ: ฟีเจอร์ที่ยังไม่ได้ทำ (หัวข้อ 8), เทสต์ที่ไม่ผ่าน, ข้อจำกัดเรื่องคีย์ AI (หัวข้อ 9)
- [ ] ไม่มีคีย์ AI หลุดใน git history
